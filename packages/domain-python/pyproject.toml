[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "chronoarb-domain"
version = "0.1.0"
description = "ChronoArb domain layer — pure business logic, value objects, and Protocol interfaces"
requires-python = ">=3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["chronoarb"]
